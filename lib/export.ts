// XLSX-Export (Phase 6: SheetJS wird hier vollständig integriert)
// Diese Datei wird in Meilenstein 6.1 ausgebaut.

export function generateLopExcel(
  items: Record<string, unknown>[],
  projectName: string,
  workspaceName: string,
  brandColor: string = '#2563EB'
): Buffer {
  // Temporärer Stub – wird durch echte SheetJS-Implementierung ersetzt
  throw new Error('xlsx dependency not installed yet')
}
